defaultNamespace: test
helm:
  version: "0.1.0"
  chart: "oci://zot-service.default.svc.cluster.local:8082/sleeper-chart"
  releaseName: "pepito"
  helmSecretName: helm-secret-test
targetCustomizations:
- name: local-test
  helm:
    version: "0.2.0"
  clusterSelector:
    matchLabels:
      name: local
