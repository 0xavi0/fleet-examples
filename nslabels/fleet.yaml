defaultNamespace: nslabel-demo

targetCustomizations:
  - name: prod
    clusterSelector:
      matchLabels:
        env: prod
    namespaceLabels:
      tier: critical
  - name: dev
    clusterSelector:
      matchLabels:
        env: dev
